---
title: "Kiritimati PPQ Data Exploration"
author: "Kevin Bruce, John Burns, Kailey Pascoe, & Julia Baum"
date: "`r format(Sys.time(),'%d %B, %Y')`"
output:
  html_document:
    toc: yes
    toc_depth: 3
    toc_float: yes
---

```{r Load Packages & Data, include=FALSE}
#rm(list=ls())
#dev.off()

#Load in Packages
library(dplyr)
library(ggplot2)
library(stats)
library(Rmisc)
library(here)
library(ggpmisc)
library(knitr)
library(magick)
library(gridExtra)

#Load in 2019 MPQ Data as a csv file
mpq19 <- read.csv(here("Data", "2019_MPQ_Complexity_Data.csv"))

#Load in 2015 & 2017 MPQ Data as csv file
mpq15.17 <- read.csv(here("Data", "MPQdata_FINAL.csv"))

#Load in MPQ values obtained by KB VS JM at select MPQ's
mpq_arc <- read.csv(here("Data", "Comparing MPQ sizes and metric changes.csv"))
mpq_arc

#Load in DEM at different scales dataframe
dem <- read.csv(here("Data", "Differing DEM's.csv"))

#Load in stakes worksheet
stakes <- read.csv(here("Data", "Stake Changes.csv"))

#Load in 2d area round 2 where I tried to get as close as possible to one another
surface <- read.csv(here("Data", "2dSA.csv"))

#Load in Variability Assessment data
variability <- read.csv(here("Data", "VariabilityTests.csv"))
colnames(variability)

#Load CloudCompare data
CC <- read.csv(here("Data", "MPQ_CloudCompare_Data.csv"))
```

```{r prep dataframes of old data, include=FALSE}
#See the colnames for each dataframe
colnames(mpq19)
colnames(mpq15.17)

#Bring Over Collumns that I want to compare
mpq.jm <- subset(mpq15.17, select=c("Site", "hum_dist_site", "Year", "PPQ","X2D_Area", "rugosity", "terrain_rug","heat_stress", "hum_dist", "curvature", "abs_curv"))

mpq.kb <- subset(mpq19, select=c("Site", "hum_dist_site", "Sample.Year", "PPQ", "SHAPE_Area","X3D.Surface.Area.Complexity", "Avg_terrain_ruggedness..VRM.","Curvature.JM.style" ,"abs_curv")) 

#Change collumn names in KB Dataframe to match JM Dataframe
colnames(mpq.kb)[which(names(mpq.kb)== "Sample.Year")] <- "Year" #change capital T to lower case t
colnames(mpq.kb)[which(names(mpq.kb)== "X3D.Surface.Area.Complexity")] <- "rugosity"
colnames(mpq.kb)[which(names(mpq.kb)== "Avg_terrain_ruggedness..VRM.")] <- "terrain_rug"
colnames(mpq.kb)[which(names(mpq.kb)== "SHAPE_Area")] <- "X2D_Area"
colnames(mpq.kb)[which(names(mpq.kb)== "Curvature.JM.style")] <- "curvature"

#Rename collumn name in dem dataframe
colnames(dem)[which(names(dem)== "DEM_Scale..cm.")] <- "DEM.scale"


#Add human_dist collumn to kb Dataframe
mpq.kb$hum_dist <- "a"

#Add heat_stress collumn to kb Dataframe
mpq.kb$heat_stress <- "After"

#Add according values to human dist collumn
for(i in c(1:nrow(mpq.kb))) {
  if(mpq.kb$Site[i]=="5") {
    mpq.kb$hum_dist[i] <- "Very Low"
  } else if(mpq.kb$Site[i]=="19") {
    mpq.kb$hum_dist[i] <- "Very Low"
  } else if(mpq.kb$Site[i]=="15") {
    mpq.kb$hum_dist[i] <- "Very Low"
  } else if(mpq.kb$Site[i]=="8") {
    mpq.kb$hum_dist[i] <- "Medium"
  } else if(mpq.kb$Site[i]=="34") {
    mpq.kb$hum_dist[i] <- "Medium"
  } else if(mpq.kb$Site[i]=="35") {
   mpq.kb$hum_dist[i] <- "Medium"
  } else if(mpq.kb$Site[i]=="27") {
   mpq.kb$hum_dist[i] <- "Very High"
  } else if(mpq.kb$Site[i]=="30") {
   mpq.kb$hum_dist[i] <- "Very High"
  } else if(mpq.kb$Site[i]=="32") {
    mpq.kb$hum_dist[i] <- "Very High"
  } else if(mpq.kb$Site[i]=="37") {
    mpq.kb$hum_dist[i] <- "Very Low"
  } else if(mpq.kb$Site[i]=="40") {
    mpq.kb$hum_dist[i] <- "High"
  } else if(mpq.kb$Site[i]=="3") {
    mpq.kb$hum_dist[i] <- "Low"
  }}


#Create Unique Col ID to match dataframes to
mpq.jm$Year.Site <- paste(mpq.jm$Year, mpq.jm$Site, sep=".")
mpq.kb$Year.Site <- paste(mpq.kb$Year, mpq.kb$Site, sep=".")

colnames(mpq.kb)
colnames(mpq.jm)


#Merge both files into one dataframe
mpq <- rbind(mpq.jm,mpq.kb)

#Make Year a factor for plotting ease
str(mpq$Year)
mpq$Year <- as.factor(mpq$Year)

#Make PPQ a Character
str(mpq$PPQ)
mpq$PPQ <- as.character(mpq$PPQ)

#Include all sites with data across 3 time point measurements
mpq2 <- subset(mpq, Site == 5| Site == 15|Site == 19|Site == 27|Site == 30|Site == 32|Site == 8|Site == 34|Site == 35)
mpq2

#Make Year a factor for plotting ease
str(mpq2$Year)
mpq2$Year <- as.factor(mpq2$Year)

#Make PPQ a Character
str(mpq2$PPQ)
mpq2$PPQ <- as.character(mpq2$PPQ)

#Make Site a character
str(mpq2$Site)
mpq2$Site <- as.character(mpq2$Site)

#Change the order of human disturbance levels for the plots
levels(mpq2$hum_dist)
mpq2$hum_dist <- factor(mpq2$hum_dist, levels = c("Very Low", "Low", "Medium", "High", "Very High" ))


#Change the order of human disturbance levels for the Site level plots
levels(mpq2$hum_dist_site)
mpq2$hum_dist_site <- factor(mpq2$hum_dist_site, levels = c("VL1", "VL2", "VL3", "VL4", "L1", "M1", "M2", "M3", "H1", "VH1", "VH2", "VH3"))

#Export Mpq2 dataframe to a csv file
write.csv(mpq2, here("Data", "2015-2019_MPQ_Data.csv"))

#Database:
mpq_arc

#Create Unique Col ID to match dataframes to
mpq_arc$Processor.Year <- paste(mpq_arc$Year, mpq_arc$Processor, sep = ".")
mpq_arc

mpq_arc$Site.PPQ <- paste(mpq_arc$Site, mpq_arc$PPQ, sep = ".")
str(mpq_arc)

#Change collumn names to make easier for plotting

#Change collumn names in KB Dataframe to match JM Dataframe
colnames(mpq_arc)[which(names(mpq_arc)== "X2D_Area")] <- "PlotArea" #change capital T to lower case t
colnames(mpq_arc)[which(names(mpq_arc)== "abs_curv")] <- "Curvature" #change capital T to lower case t



#Database:
dem

#Make DEM Scale a factor 
dem$DEM.scale <- as.character(dem$DEM.scale)

#In the DEM dataframe, add a collumn for hum.dist
dem$hum_dist <- "a"

#Add according values to human dist collumn
for(i in c(1:nrow(dem))) {
  if(dem$Site[i]=="5") {
    dem$hum_dist[i] <- "VL3"
  } else if(dem$Site[i]=="27") {
    dem$hum_dist[i] <- "VH1"
  } else if(dem$Site[i]=="35") {
    dem$hum_dist[i] <- "M2"
  }}

str(dem)



#Create a unique collumn for hum_dist and PPQ
dem$site.ppq <- paste(dem$hum_dist, dem$PPQ, sep = ".")


# #Change the order of human disturbance levels for the Site level plots
# levels(dem$hum_dist)
# dem$site.ppq <- factor(dem$site.ppq, levels = c("VL3.1", "VL3.2", "M2.1", "M2.2","VH1.1", "VH1.2"))


##Stakes dataframe##
colnames(stakes)

#Change collumn names in stakes 
colnames(stakes)[which(names(stakes)== "X..of.new.stakes.2017...2019.")] <- "stakes.lost" 

levels(stakes$hum_dist)
stakes$hum_dist <- factor(stakes$hum_dist, levels = c("VL","M","H","VH"))

##2d surface area changes dataframe##
surface.area <- subset(surface, select=c("Year", "Site", "PPQ", "SHAPE_Area"))
  
colnames(surface)

#only include 2017 and 2019 years
surface.area <- subset(surface.area, Year == 2017| Year == 2019)

#Change the structure of some of the collumns in the surface.area dataframe
str(surface.area)
surface.area$Year <- as.factor(surface.area$Year)
surface.area$PPQ <- as.factor(surface.area$PPQ)

#Create a unique collumn for Year and PPQ
surface.area$Year.PPQ <- paste(surface.area$Year, surface.area$PPQ, sep = ".")



##For the followup results removing a few sites
#Create new dataframe to then delete out specific sites
mpq3 <- mpq2

#Create new collumn to 
mpq3$site.ppq <- paste(mpq3$hum_dist_site, mpq3$PPQ, sep = ".")
mpq3

#Only include certain ppq's in the new dataframe (exclude: VH2 PPQ 1,2,3; VH3 PPQ3; M1 PPQ2 (for Rugosity), and also VH1 PPQ2; M2 PPQ3 for Terrain Ruggedness)

ilike <- c("VL3.1" ,"VL3.2", "VL3.3" ,"M1.1" , "M1.3",  "VL1.1", "VL1.2" ,"VL1.3" ,"VL2.2", "VL2.3", "VH1.1", "VH1.3" ,"VH3.1", "VH3.2" ,"M3.1",  "M3.2" , "M3.3" , "M2.1" , "M2.2")
  

mpq4 <- subset(mpq3, site.ppq %in% ilike)

##Making plots including sites sampled ALL 3 timepoints##

#Only include sites with 3 time point measurements
mpq5 <- subset(mpq2, Site == 5 |Site == 27|Site == 30|Site == 32|Site == 8|Site == 34|Site == 35)
mpq5


### Organizing dataframe of varying dem's to graph ### 
str(dem)
str(dem$Year)
dem$Year <- as.character(dem$Year)

#Add according values to human dist collumn
for(i in c(1:nrow(dem))) {
  if(dem$Site[i]=="5") {
    dem$hum_dist[i] <- "Very Low"
  } else if(dem$Site[i]=="27") {
    dem$hum_dist[i] <- "Very High"
  } else if(dem$Site[i]=="35") {
    dem$hum_dist[i] <- "Medium"
  }}

#Change the order of human disturbance levels for the plots
levels(dem$hum_dist)
dem$hum_dist <- factor(dem$hum_dist, levels = c("Very Low", "Medium", "Very High" ))


# #Change the order of human disturbance levels for the Site level plots
# levels(dem$hum_dist)
# dem$site.ppq <- factor(dem$site.ppq, levels = c("VL3.1", "VL3.2", "M2.1", "M2.2","VH1.1", "VH1.2"))


###Variability tests###
#Create a unique ID collumn for Site.PPQ
variability$site.ppq <- paste(variability$Site, variability$PPQ, sep=".")
variability



```

# Initial Data sent to JB on Feb 26, 2020
## Introduction 
Kiritimati Research Project- PPQ rugosity analyses 2015 - 2019


PPQ = Permenant Photoquadrat (4m X 4m)

This document is for the PPQ data collected on Kiritimati to look at rugosity changes that occur on reefs following a mass mortality event.

The plots below show a view of the data across different spatial and temporal scales. We will start by looking at the data braodly before zooming in to specific PPQ fluctuations over time. 

It is important to note that Sites 15 and 19 (both within the Very Low disturbance category) were unable to be sampled in 2019 due to weather, however Site 37 in Vaskess Bay was accessible and used as a Very Low site (VL4) along with Site 5 (VL3).

For reference, here is the map of Kiritimati listing the sites that I plan to use for the manuscript. Sites H1, H2, and L1 haven't been used in these analyses as there is no Agisoft Model created to compare yet. (all will be compared to levels post-El Niño). 


```{r map, echo=FALSE, fig.height=11, fig.width=7, fig.align="center"}
map <- image_read("Map/KI_map_sites_villages_inset_KB_MSc_Ch1.png")
map

```

Below is a table indicating the various sites around Kiritimati and how many MPQ's were sampled during each expedition

```{r Table for Julia, echo=FALSE, fig.align="center"}
map4 <- image_read("Map/sites_sampled_table.png")
map4

```



## Year level
The three rugosity metrics are averaged to the year level, showing each metric's changes across the three time points. For each metric, I've included the graph of all available sites on the left, with the graph showing only the sites with data from all three timepoints (2015, 2017, 2019). 

These sites include:

VH 1,2,3

M 1,2,3

VL 3


### Rugosity 

```{r Rugosity by year, echo=FALSE}
plot10 <- ggplot(mpq2, aes(x = Year, y=rugosity)) + ggtitle("All sites") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

plot11<- ggplot(mpq5, aes(x = Year, y=rugosity)) + ggtitle("3 timepoint sites only") + geom_boxplot(fill="forestgreen") + theme_classic(base_size = 10) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

#Compare plots side by side
grid.arrange(plot10, plot11, ncol=2)

```

```{r Rug by year Presentation graph, eval=FALSE, include=FALSE}
ggplot(mpq2, aes(x = Year, y=rugosity)) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14, base_family = "Times New Roman") + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(panel.grid.major = element_blank(),panel.grid.minor = element_blank(),  
        #legend.position=c(0.15, 0.89), 
        plot.background=element_blank(), 
        panel.background = element_blank() ,
        axis.text = element_text(color="black", size = 18), 
        axis.ticks=element_line(color = "black"),axis.ticks.length = unit(0.2, "cm"), axis.line = element_line(color = "black"), 
        axis.title = element_text(color = "black", size = 26))
```

There as a large decline between 2015 and 2017, however it appears to level out when comparing 2017 and 2019.

### Terrain Ruggedness
```{r TR/yr, echo=FALSE}
plot12 <- ggplot(mpq2, aes(x = Year, y=terrain_rug)) + ggtitle("All sites") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

plot13<- ggplot(mpq5, aes(x = Year, y=rugosity)) + ggtitle("3 timepoint sites only") + geom_boxplot(fill="forestgreen") + theme_classic(base_size = 10) + xlab("Year") + ylab("Terrain Ruggedness ")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

#Compare plots side by side
grid.arrange(plot12, plot13, ncol=2)

```


```{r TR by year Presentation graph, eval=FALSE, include=FALSE}
ggplot(mpq2, aes(x = Year, y=terrain_rug)) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14, base_family = "Times New Roman") + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(panel.grid.major = element_blank(),panel.grid.minor = element_blank(),  
        #legend.position=c(0.15, 0.89), 
        plot.background=element_blank(), 
        panel.background = element_blank() ,
        axis.text = element_text(color="black", size = 18), 
        axis.ticks=element_line(color = "black"),axis.ticks.length = unit(0.2, "cm"), axis.line = element_line(color = "black"), 
        axis.title = element_text(color = "black", size = 26))
```


Similar results that were seen with surface rugosity, however it seems that Terrain Ruggedness  increases from its lowest point in 2017. 

### Curvature
```{r Abs_curv/yr, echo=FALSE}
plot14 <- ggplot(mpq2, aes(x = Year, y=rugosity)) + ggtitle("All sites") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

plot15<- ggplot(mpq5, aes(x = Year, y=rugosity)) + ggtitle("3 timepoint sites only") + geom_boxplot(fill="forestgreen") + theme_classic(base_size = 10) + xlab("Year") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

#Compare plots side by side
grid.arrange(plot14, plot15, ncol=2)

```

Similar to Terrain Ruggedness, absolute curvature increases from 2017 to 2019, although the variation between the sites in 2019 is higher than in 2017. 



## Disturbance gradient level
The three rugosity metrics are now averaged by disturbance gradient, which in this case has 3 levels: Very Low, Medium, Very High. These levels are pre-determined by previous publications taking into effect the fishing pressure and distance from the human populous.

For each metric there will be two graphs. The first is including all applicable sites with data from 2015, 2017, and 2019. The second graph only includes sites that have data from all 3 timepoints, meaning that the VL sites only has 1 site on these graphs as VL1 and VL2 weren't accessible in 2019 due to weather. 


### Rugosity
```{r rug by dist.lvl, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist, y=rugosity, fill = Year)) + ggtitle("Surface rugosity by human disturbance gradient") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))
```




```{r rug by dist. level for 3 disturbance times, echo=FALSE}
ggplot(mpq5, aes(x = hum_dist, y=rugosity, fill = Year)) + ggtitle("3 timepoint sites only") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```

```{r rug by dist.lvl for presentation, eval=FALSE, include=FALSE}
ggplot(mpq2, aes(x = hum_dist, y=rugosity, fill = Year)) +  geom_boxplot() + theme_classic(base_size = 14, base_family = "Times New Roman") + xlab("Disturbance Level") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(panel.grid.major = element_blank(),panel.grid.minor = element_blank(),  
        #legend.position=c(0.15, 0.89), 
        plot.background=element_blank(), 
        panel.background = element_blank() ,
        axis.text = element_text(color="black", size = 18), 
        axis.ticks=element_line(color = "black"),axis.ticks.length = unit(0.2, "cm"), axis.line = element_line(color = "black"), axis.title = element_text(color = "black", size = 26))

```


Results show a similar trend to what we witnessed earlier when looking at the metrics by year, in that 2017 and 2019 values appear relatively similar, or even with 2019 values slightly increased. The Medium and Very Low sites appear to be driving the overall increase seen in 2019, with the Very High sites experiencing similar values to 2017. 



### Terrain Ruggedness
```{r TR/hum, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist, y=terrain_rug, fill = Year)) + ggtitle("TR all sites") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))
```


```{r TR/hum 3 timepoints, echo= FALSE}
ggplot(mpq5, aes(x = hum_dist, y=terrain_rug, fill = Year)) + ggtitle("TR 3 timepoint sites only") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))
```


```{r TR by dist.lvl for presentation, eval=FALSE, include=FALSE}
ggplot(mpq2, aes(x = hum_dist, y=terrain_rug, fill = Year)) +  geom_boxplot() + theme_classic(base_size = 14, base_family = "Times New Roman") + xlab("Disturbance Level") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(panel.grid.major = element_blank(),panel.grid.minor = element_blank(),  
        #legend.position=c(0.15, 0.89), 
        plot.background=element_blank(), 
        panel.background = element_blank() ,
        axis.text = element_text(color="black", size = 18), 
        axis.ticks=element_line(color = "black"),axis.ticks.length = unit(0.2, "cm"), axis.line = element_line(color = "black"), axis.title = element_text(color = "black", size = 26))

```

Here we actually see all 3 human disturbance gradients actually experienced an increase in Terrain ruggedness in 2019 vs 2017. Is it possible to keep the structure intact and increase TR? TR = grooves and valleys of the surface, so does this potentially mean that there are more of these appearing on the reef?



### Curvature
```{r Curv/Human, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist, y=abs_curv, fill = Year)) + ggtitle("Curvature all sites") + geom_boxplot()+ theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```


```{r Curv/human 3 timpeoints, echo=FALSE}
ggplot(mpq5, aes(x = hum_dist, y=abs_curv, fill = Year)) + ggtitle("Curvature 3 timepoint sitess only") + geom_boxplot()+ theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```

Huge variation in the 2019 medium disturbance gardient, with very high experiencing expected curvature values. Very low experienced an increase in curvature values in 2019 to even pre-El nino levels, however site differences may play a role on this one too. 







## Site level
Here I averaged the rugosity metrics by site for each time point sampled. At this level, declines from 2015 to 2017 were noted as in JM's paper, but many of the 2019 values seem to increase. Unsure what is going on?

### Rugosity
```{r Rugosity by Site over time, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist_site, y=rugosity, fill = Year)) + ggtitle("Rugosity changes over time at each site") + geom_boxplot()+ theme_classic(base_size = 14) + xlab("Site") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```


Some sites are experiencing increases in surface rugosity to levels even higher than in 2015 (Ex: Site VH2) with many others having larger values than in 2017 (Ex: VL3, M2). These discrepencies span across the disturbance gradient as well. 


### Terrain Ruggedness
```{r Terrain Ruggedness over time by site, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist_site, y=terrain_rug, fill = Year)) + ggtitle("Terrain Ruggedness changes over time at each site") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Site") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=20))
```


Same as surface rugosity notes, with site VH2 still having larger TR values in 2019 than in 2015. 

### Curvature
```{r curvature at each site and time period, echo=FALSE}
ggplot(mpq2, aes(x = hum_dist_site, y=abs_curv, fill = Year)) + ggtitle("Absolute Curvature changes over time at each site") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Site") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=20))
```


Curvature is a bit of an oddball, as a it appears the variation between sites is huge (very long boxplots), with sites VL3 and M2 having larger values in 2019 than in 2015. Doesn't seem to be a discernable pattern here that I can tell. 



## PPQ level
The rugosity metrics are brought down to the PPQ level, with metric values at each of the three time points the PPQ's were sampled. Sites 15 and 19 were unable to be reached (due to weather) in 2019, hence the two time points shown.

### Rugosity 
```{r Each PPQ for each site at each time point, echo=FALSE}
ggplot(mpq2, aes(PPQ, rugosity, colour = Year)) + geom_boxplot() +facet_wrap(~hum_dist_site, scales = "fixed", ncol= 3) + xlab("PPQ") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```

For a few sites and PPQ's, rugosity has declined, or stayed stable since 2017 [as expected] but, for some PPQ's and sites, rugosity seems to increase to 2015 levels or even exceeding them. Eg: Site 32. Not sure how this could be the case? 



### Terrain Ruggedness
```{r TR at each site at each time point, echo=FALSE}
ggplot(mpq2, aes(PPQ, terrain_rug, colour = Year)) + geom_boxplot() +facet_wrap(~hum_dist_site, scales = "fixed", ncol= 3) + xlab("PPQ") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))
```

Similar issue to Rugosity at the PPQ level, some sites are higher in 2019 than in 2017, with site 30 PPQ3 even higher than pre-el Nino in 2015. Site 32 as a whole experienced this, but we are looking at the orthos for each plot to see if we can visually see a difference (hypothesis is that this high disturbance sites have lots of loose abiotic material movement throughout the year)

### Curvature
```{r curvature at each PPQ, echo=FALSE}
ggplot(mpq2, aes(PPQ, abs_curv, colour = Year)) + geom_boxplot() +facet_wrap(~hum_dist_site, scales = "fixed", ncol= 3) + xlab("PPQ") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```


Similar problem, except here its site 35 PPQ 1 & 2, Site 5 PPQ1 that have their highest curvature vales in 2019. Why? 




## Surface Area of each PPQ at each time point
Could the different values we are getting over the 3 time points be because we are not exactly photographing the same area, despite the steel stakes being there? Divers did the best they could in ensuring the location of new, replacement stakes from 2016 onwards were located in the same area as the intial stake, but given these results, it appears they weren't as sucessful as hoped. 


Here is the plot area of all plots, showing how they change over time.

```{r Each PPQs 2D Surface Area at each time point, echo=FALSE}
ggplot(mpq2, aes(PPQ, X2D_Area, colour = Year)) + geom_boxplot() +facet_wrap(~Site, scales = "fixed", ncol= 3) + xlab("PPQ") + ylab("2D Plot Surface Area")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))



#Create new dataframe for just area measurements
area1 <- subset(mpq2, select=c("Site","Year", "PPQ","X2D_Area"))
area1$x2d_rounded <-"0"

#Add rounded values to dataframe
area1$x2d_rounded <- round(mpq2$X2D_Area)

#only include 2019 PPQ's
area <- subset(area1, Year ==2019)

#plot barchart of x2d_round values
ID <- 10:30

ggplot(area, aes(x2d_rounded)) + geom_bar()+ theme(axis.text.x = element_text(angle = 90, vjust = 0.5)) +
  scale_x_continuous("ID", labels = as.character(ID), breaks = ID)


```

In theory, the PPQ's should all be the same (~16m2 in area), however you can see that there is large amounts of variability between sample time points. This could potentially account for some of the variability that we are seeing in the previous rugosity measurements, however I'm not really sure where to go from here. Any idea how to fix this or experience with this in the past? 


## Plot 2D Surface Area Comparissons
While those values are similar, the area of each plot is different between processors as KB went to the base of the metal stakes and JM went to the centre of the reflectors, as shown below by the differing areas of the plots. 

```{r Area Measurements, echo=FALSE}
ggplot(mpq_arc, aes(x= Processor.Year, y= PlotArea, fill=Processor.Year)) +  geom_bar(stat = 'identity') + facet_grid(~Site.PPQ) +  xlab("Year and Processor") + ylab("2D Plot Surface Area")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))+ theme(axis.text.x = element_text(angle = 45, hjust = 1))

```

This plot highlights the differences in plot area that occur based on where the corners of the plot are digitized in ArcMap. JM's digitized vertices were located on the reflectors while KB's are at the base of the metal stakes. In conclusion, KB should redo JM's plots form 2015 and 2017 to have the plot vertices at the base of the metal stakes vs the reflectors.


## ArcMap output comparissons
These plots were done to make sure the differences we were seeing in rugosity, TR, and cuvature weren't different due to user error. KB compared the same 3 sites Surface Rugosity and Terrain Ruggedness values to those reported by JM and found that his values were very similar to hers. Note: Values may be slightly off as KB analyzed his plot area from the base of the metal stakes, whereas JM analyzed her plots from the centre of the reflectors. 

### Rugosity 
```{r Rugosity KB vs. JM, echo = FALSE}
ggplot(mpq_arc, aes(x= Processor.Year, y= rugosity, fill=Processor.Year)) +  geom_bar(stat = 'identity') + facet_grid(~Site.PPQ) + xlab("Year and Processor") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(axis.text.x = element_text(angle = 45, hjust = 1)) + theme(plot.title = element_text(size=22))

```

KB and JM's values are close, however 5.2 (Site5-PPQ2) and 8.3 (Site 8-PPQ3) are off enough that KB should go back and redo JM's metric calculations from the base of the stakes.

### Terrain Ruggedness
```{r TR KB Vs. JM, echo=FALSE}
ggplot(mpq_arc, aes(x= Processor.Year, y= terrain_rug, fill=Processor.Year)) +  geom_bar(stat = 'identity') + facet_grid(~Site.PPQ) + xlab("Year and Processor") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(axis.text.x = element_text(angle = 45, hjust = 1))


```

Values appear consistent between processor, however with rugosity being different, values collected for JM's paper will need to be redone for this one from the base of the metal stakes, not from the plastic reflectors. 

### Curvature
```{r Curvature values KB Vs. JM, echo=FALSE}
ggplot(mpq_arc, aes(x= Processor.Year, y= Curvature, fill=Processor.Year)) +  geom_bar(stat = 'identity') + facet_grid(~Site.PPQ) + xlab("Year and Processor") + ylab("Absolute Curvature")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(axis.text.x = element_text(angle = 45, hjust = 1))


ggplot(mpq_arc, aes(x= Processor.Year, y= rugosity, fill=Processor.Year)) +  geom_bar(stat = 'identity') + facet_grid(~Site.PPQ) + xlab("Year and Processor") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(axis.text.x = element_text(angle = 45, hjust = 1)) + theme(plot.title = element_text(size=22))

```

Similar to rugosity results, where the differences between measurements are too high to use JM's values from her results. Unsure as to what is causing these discrepencies. Additionally, while these values align with similar values to what JM has received, they are not what ArcMAP help uses to describe Curvature. Is there a way that we can scale these where we are able to set the range of values to a range used in other publicized studies, from say -1 to 1? 



## Future work
1. Potentially look at how the planiform and profile curavtures differ across time points? I was reading that profile curvature affects the acceleration and deceleration fo flow ==> Perhaps higher plots experiencing higher values of this will experience faster erosion rates?

2. At a conference I just attended, a post-doc inquired whether Ocean Acidification could play a role on this. Perhaps it is eroding the rock and opening up more of those "nooks and crannies", thereby enabling the TR values to increase?



--------------------------------------------
# Follow-up to JB's response

John replied on March 2, 2020 with questions regarding the preliminary results sent out (my findings I list directly below the question)



**1. Am I noticing any algae growth or calcifying animals (ex: CCA) in the 2019 plots?**

Nothing significant. If anything, there are large matts of macroalgae within the 2017 plots that have dissappeared mostly in the 2019 plots, so shouldn't the values decrease?


**2. Can you confidently assess the plot areas visually by looking at the mosaics?**

For some of the sites, with the stakes located staying consistent between timepoints (IE: no stakes were replaced between two years), it is possible however there is still minute observor error in arcmap as the placement of the vertices obviously isn't the same (as shown in the graph below). With the Agisoft files from John, this will hopefully be negated as you can filter photos by points and place the vertice at the base of the stake as closely as possible, however there will still be some variation in overall size involved.


```{r Differing Surface areas between timepoints, echo=FALSE}
ggplot(surface.area, aes(Year.PPQ, SHAPE_Area, fill=Year.PPQ)) + geom_col() + facet_wrap(~Site, scales = "fixed", ncol= 3) + xlab("Year & PPQ") + ylab("2D Plot Surface Area") + theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))+ theme(axis.text.x = element_text(angle = 45, hjust = 1)) + theme(plot.title = element_text(size=22))
```


However, sites in the very high human disturbance levels experienced the most stakes removed between 2017 and 2019, so replicating this evenly across all PPQ's will be very difficult (or impossible in sites there 3 of the stakes needed to be replaced in 2019)

```{r stakes lost, echo=FALSE}
ggplot(stakes, aes(x= hum_dist, y= stakes.lost, fill = hum_dist)) + geom_bar(stat = 'identity') +  xlab("Human Disturbance Gradient") + ylab("Stakes Lost between 2017 - 2019")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```


You can see the amount of abiotic rubble that moves between years in the below image. 2017 is on the left and 2019 is on the right of VH2-PPQ1. 


```{r map2, echo=FALSE, fig.height=11, fig.width=7, fig.align="center"}
map3 <- image_read("Map/VH2_MPQ1_2017_2019.png")
map3

```

The only reason I can ensure this is the correct layout as the seabird was consistent in the 2 years (on the stake the transect line starts on) and the other stake (bottom left corner in 2017) was the only other remaining permenant stake in 2019. Some sites within the VH disturbance gradient experienced this and some even lost 3 stakes over those 2 years, making it incredibly difficult to accurately place the true corners over time. 


**3. Assess the photomosaics among the years and see if the shape is different due to the orientation of the camera when the models were made. **

I have done that and all photomosaics appear to be from the same view (birdseye). However the DEM scales from some of the 2015 mosaics are not set to 0.01 so I will redo those assessments once the hard drive arrives at UVIC. 


**4. Have you tried statistically analyzing the differences in the PDF?**

Not yet, I would like some direction on what tests to run (ANOVA?) or whether running a model would be best (statistics isn't my stronest subject).

## Removing mosaics of concern: do trends remain?
**5. Check all mosaics, remove the ones of concern and then assess the trends again.**

I removed the suspect sites: VH2:PPQ 1,2,3; M1: PPQ2; VH3:PPQ3, VH1 PPQ2 & M2 PPQ3. 

These were chosen by looking at the indicdiaul PPQ measurements from the intial results and removed based on those results for Surface rugsoity and Terrain Ruggedness. 

For all metrics, the blue graphs indicate the intial results presented, while the red graph on the righ shows the results with the above sites removed.

### Surface Rugosity by year

```{r Removed bad sites, echo=FALSE}
plot1<- ggplot(mpq2, aes(x = Year, y=rugosity)) + ggtitle("SR  by year") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

plot2 <- ggplot(mpq4, aes(x = Year, y=rugosity)) + ggtitle("SR  by year (missing sites)") + geom_boxplot(fill="red") + theme_classic(base_size = 14) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

#Compare plots side by side
grid.arrange(plot1, plot2, ncol=2)


```

The trend appears to slightly dissapear in the surface rugosity data, with 2019 being very similar to that of 2017. Next step would be to statistically test if any are significantly different.

### Terrain Ruggedness by year

```{r TR removing sites, echo=FALSE}

plot3<- ggplot(mpq2, aes(x = Year, y=terrain_rug)) + ggtitle("TR by year") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

plot4 <- ggplot(mpq4, aes(x = Year, y=terrain_rug)) + ggtitle("TR by year (missing sites)") + geom_boxplot(fill="red") + theme_classic(base_size = 14) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

#Compare plots side by side
grid.arrange(plot3, plot4, ncol=2)
```

The TR trend remains consistent between both graphs, indicating that removing the suspect sites didn't alter our results.


### Absolute Curvature by year

```{r abs_curv removed sites, echo=FALSE}
plot5<- ggplot(mpq2, aes(x = Year, y=terrain_rug)) + ggtitle("AbsCurv by year") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

plot6 <- ggplot(mpq4, aes(x = Year, y=terrain_rug)) + ggtitle("AbsCurv by year (missing sites)") + geom_boxplot(fill="red") + theme_classic(base_size = 14) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))

#Compare plots side by side
grid.arrange(plot5, plot6, ncol=2)


```

Similar to TR, absolute curvature's trend remained similar between samples.


## Differing DEM sizes
**6. Does the rugosity value trends stay consistent with what we are already seeing with differing sized DEM's? **

Yes it does, as you can see in the graphs below. I scaled out the DEM's from 3 sites within 3 separate human disturbance gradients (VH1, M2, VL3). The value after the site indicates the PPQ that was sampled. Each box represents a certain's site PPQ (ex: VL3.PPQ1) from sites that weren't removed from the dataset from point #5. 


### Surface Rugosity @ differing DEMs
```{r DEMs at varying scales on same plot}
ggplot(dem, aes(x = hum_dist, y=Rugosity)) + facet_wrap(~DEM.scale, scales = "fixed", ncol= 3) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + ggtitle("2019 Surface Rugosity @ different DEM Scales") + xlab("Human Disturbance") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))

```


Surface rugosity decreased as the DEM's got larger across the board, with the lowest SR values seen at 8cm DEM's.

### Terrain Ruggedness @ differing DEMs
```{r TR DEMs, echo=FALSE}
ggplot(dem, aes(x = hum_dist, y=Terrain.Ruggedness)) + facet_wrap(~DEM.scale, scales = "fixed", ncol= 3) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + ggtitle("2019 Terrain Ruggedness @ different DEM scales") + xlab("Human Disturbance") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=16))
```

Interestingly, it appears that DEM's at scale 4 had the highest TR values of the three in most of the PPQ's. 


### Absoulute Curvature @ differing DEMs
```{r Abs_curv DEMs, echo=FALSE}
ggplot(dem, aes(x = hum_dist, y=Abs.Curv)) + facet_wrap(~DEM.scale, scales = "fixed", ncol= 3) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 14) + ggtitle("2019 Absolute Curvature @ different DEM scales") + xlab("Human Disturbance") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))
```

Interestingly, medium disturbance sites exhibit the highest curvature values by far across the disturbance gradient. 
 
 
## Interesting find

In looking at the mosaics throughout the years, it was intesting to notice certain corals and realize how much they grow over the time. This below coral shows the coral in 2017(left) and 2019 (right), indicating pretty large amounts of growth. Perhaps this could be a portion of a potential 2nd chapter? 

```{r coral growth noticed,  echo=FALSE}
map2 <- image_read("Map/Coral_Growth_2017_Vs_2019.png")
map2
```

Coral Area covered in 2017: 0.051601

Longest radius and diamter: 32cm X 18cm



Coral Area covered in 2019: 0.092862

Longest radius and diamter: 38cm X 30cm


## Questions for John

1. Is there a way to set a range of outputs for curvature and TR? Where 0 = flat and higher values mean more rugged or indicate higher values of curvature until a maximum which is basically a cliff (for Rugosity example)

2. 


### NEW WORK APRIL 2020 ###

```{r variability test, echo=FALSE}
variability
colnames(variability)

#Round X2D Surface Area to nearest whole number
variability$X2D_Area <- round(variability$X2D_Area)

#Make 2D SA a character
str(variability)
variability$X2D_Area <- as.character(variability$X2D_Area)


#Rugosity
ggplot(variability, aes(x= X2D_Area, y= Rugosity)) +  geom_bar(stat = 'identity') + facet_grid(~site.ppq) +  xlab("2D Plot Surface Area") + ylab("Rugosity")

#VRM
ggplot(variability, aes(x= X2D_Area, y= VRM)) +  geom_bar(stat = 'identity') + facet_grid(~site.ppq) +  xlab("2D Plot Surface Area") + ylab("VRM")

#Curvature
ggplot(variability, aes(x= X2D_Area, y= Curv)) +  geom_bar(stat = 'identity') + facet_grid(~site.ppq) +  xlab("2D Plot Surface Area") + ylab("Curvature")

#Pro Curvature
ggplot(variability, aes(x= X2D_Area, y= Curv.Pro)) +  geom_bar(stat = 'identity') + facet_grid(~site.ppq) +  xlab("2D Plot Surface Area") + ylab("Profile Curvature")

#Plan Curvature
ggplot(variability, aes(x= X2D_Area, y= Curv.Plan)) +  geom_bar(stat = 'identity') + facet_grid(~site.ppq) +  xlab("2D Plot Surface Area") + ylab("Planiform Curvature")
```


```{r committee meeting #1 plots}
#Load in MPQ Data as a csv file
mpq.all <- read.csv(here("Data", "CommitteeData.csv"))

#Restructure data so that it matches it
str(mpq.all)
mpq.all$Year <- as.factor(mpq.all$Year)

#Re-order the levels of human disturbance so that it graphs nicely
mpq.all$Hum_Dist <- factor(mpq.all$Hum_Dist, levels = c("Very Low", "Medium", "Very High"))

#Plot for rugosity
ggplot(mpq.all, aes(x = Year, y=surface.rugosity)) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14)) + annotate('text', 1, 2.5, label="n = 21", size=3.5) + annotate('text', 2, 2.5, label="n = 21", size=3.5) + annotate('text', 3, 2.5, label="n = 21", size=3.5)

#Plot for terrain.ruggedness
ggplot(mpq.all, aes(x = Year, y=terrain.rug)) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5)) + theme(plot.title = element_text(size=14)) + annotate('text', 1, 0.1, label="n = 21", size=3.5) + annotate('text', 2, 0.1, label="n = 21", size=3.5) + annotate('text', 3, 0.1, label="n = 21", size=3.5)

#Plot for rugosity across human disturbance
ggplot(mpq.all, aes(x = Hum_Dist, y=surface.rugosity, fill = Year)) + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + annotate('text', 1, 2.5, label="n = 9", size=3.5) + annotate('text', 2, 2.5, label="n = 27", size=3.5) + annotate('text', 3, 2.5, label="n = 27", size=3.5)


#Plot for terrain ruggedness across human disturbance
ggplot(mpq.all, aes(x = Hum_Dist, y=terrain.rug, fill = Year)) + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Terrain Ruggedness")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + annotate('text', 1, 0.1, label="n = 9", size=3.5) + annotate('text', 2, 0.1, label="n = 27", size=3.5) + annotate('text', 3, 0.1, label="n = 27", size=3.5)


#Plot for slope
ggplot(mpq.all, aes(x = Year, y=slope)) + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Year") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))

ggplot(mpq.all, aes(x = Hum_Dist, y=slope, fill = Year)) + geom_boxplot() + theme_classic(base_size = 14) + xlab("Disturbance Level") + ylab("Surface Rugosity")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) 

```


# CloudCompare Data
# Volumetric analysis of the reef plots on Christmas Island between the timepoints from 2015 - 2019.

# Volumetric change between 2015-2017 and 2015-2019 was quantified in CloudCompare, with the change between 2017 - 2019 calculated algabraeically from the equation below:
    ## 2017 - 2019 Change = (2015-2019 Change) - (2015-2017 Change)

```{r Clean CloudCompare Data, include=FALSE}
#Remove the extra collums in the cloudcompare data that aren't necessary
CC <- subset(CC, select = c("hum_dist","timepoint", "site", "mpq", "volume.change" ))
CC

#Delete the extra rows in the dataframe with NA's
CC <- CC[-c(72,73,74,75,76,77,78,79,80),]
CC

#Check structure of CC
str(CC)   ###human_dist is a factor, which we need to change to a character

#Change hum_dist to a character
CC$hum_dist <- as.character(CC$hum_dist)
str(CC)

#Assign levels to CC$hum_dist
levels(CC$hum_dist)
CC$hum_dist <- factor(CC$hum_dist, levels = c("Very Low", "Medium", "Very High"))
levels(CC$hum_dist)


#Change the order of human disturbance levels for the plots
levels(mpq2$hum_dist)
mpq2$hum_dist <- factor(mpq2$hum_dist, levels = c("Very Low", "Low", "Medium", "High", "Very High" ))

#Change timepoint information from A, B,C to year differences
CC$timepoint <- factor(CC$timepoint, levels =c("A", "B", "C"), labels = c("2015-2017", "2017-2019", "2015-2019"))

```


```{r Plot CloudCompare Data By Timepoints, echo=FALSE}
ggplot(CC, aes(x = timepoint, y=volume.change)) + ggtitle("Volume Change by timepoint") + geom_boxplot(fill="deepskyblue") + theme_classic(base_size = 10) + xlab("Timepoint") + ylab("Volume Change (m3)")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=14))+ theme(plot.title = element_text(size=22))+ geom_hline(yintercept = 0, linetype="dashed", color="red", size=0.2)

```

```{r Plot CC Data by Timepoint & disturbance, echo=FALSE}
ggplot(CC, aes(x = timepoint, y=volume.change, fill = hum_dist)) + ggtitle("Volumetric Change by disturbance level") + geom_boxplot() + theme_classic(base_size = 14) + xlab("Timepoint") + ylab("Volumetric Change(m3)")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22))+ geom_hline(yintercept = 0, linetype="dashed", color="red", size=0.2)
       
```

```{r CC Data facetwrapped by timepoint, echo=FALSE}
ggplot(CC, aes(hum_dist, volume.change, fill = hum_dist)) + geom_boxplot() +facet_wrap(~timepoint, scales = "fixed", ncol= 3) + xlab("Human Disturbance Level") + ylab("Volumetric Change(m3)")+ theme(plot.title = element_text(hjust = 0.5))+ theme(plot.title = element_text(size=22)) + theme(plot.title = element_text(size=22))+ geom_hline(yintercept = 0, linetype="dashed", color="red", size=0.2)+ theme(axis.text.x = element_text(angle = 45, vjust = 1.1, hjust=1))

```


# MPQ Structural Complexity Work
 
